# =============================================================================
# .env.sample - 환경변수 샘플 파일
# =============================================================================
#
# 사용 방법:
# 1. 이 파일을 복사하여 .env 파일로 저장합니다
#    cp .env.sample .env
#
# 2. .env 파일에서 실제 값으로 변경합니다
#
# 주의사항:
# - .env 파일은 절대 Git에 커밋하면 안 됩니다! (.gitignore에 등록되어 있음)
# - 이 .env.sample 파일은 다른 개발자들이 어떤 환경변수가 필요한지 알 수 있도록
#   샘플값과 함께 Git에 커밋합니다
# =============================================================================

# -----------------------------------------------------------------------------
# 파일 업로드 설정
# -----------------------------------------------------------------------------
# 업로드된 파일이 저장될 폴더 경로
# 상대 경로(./uploads) 또는 절대 경로(/var/uploads) 모두 가능
FILE_UPLOAD_DIR=./uploads

# -----------------------------------------------------------------------------
# 서버 설정
# -----------------------------------------------------------------------------
# 서버가 실행될 포트 번호
SERVER_PORT=8080
