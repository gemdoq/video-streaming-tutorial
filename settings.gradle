/*
 * =============================================================================
 * settings.gradle - Gradle 프로젝트 설정 파일
 * =============================================================================
 *
 * 이 파일의 역할:
 * - 프로젝트의 이름을 정의합니다
 * - 멀티 모듈 프로젝트의 경우 하위 프로젝트들을 포함시킵니다
 *
 * 이 파일은 Gradle이 빌드를 시작할 때 가장 먼저 읽는 파일입니다.
 * build.gradle보다 먼저 실행됩니다.
 */

// rootProject.name: 프로젝트의 루트 이름을 설정합니다
// 이 이름은 JAR 파일명, 빌드 출력물 등에 사용됩니다
rootProject.name = 'video-streaming-tutorial'
